<h1>Listing user_addons</h1>

<table>
  <tr>
    <th>User</th>
    <th>Addon</th>
    <th>Level credits spent</th>
    <th>Dollars spent</th>
    <th>Activated</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_addons.each do |user_addon| %>
  <tr>
    <td><%= user_addon.user_id %></td>
    <td><%= user_addon.addon_id %></td>
    <td><%= user_addon.level_credits_spent %></td>
    <td><%= user_addon.dollars_spent %></td>
    <td><%= user_addon.activated %></td>
    <td><%= link_to 'Show', user_addon %></td>
    <td><%= link_to 'Edit', edit_user_addon_path(user_addon) %></td>
    <td><%= link_to 'Destroy', user_addon, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User addon', new_user_addon_path %>
